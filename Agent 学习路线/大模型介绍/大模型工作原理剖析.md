*大模型的核心任务：预测下一个词*

![自回归生成的循环过程|377](https://javaup.chat/generated/plantuml/bb9b9a9bb716fb938f59.svg)

# Transformer：大模型背后的架构

在 Transformer 之前，处理语言主要用 RNN（循环神经网络）。RNN 的工作方式是一个词一个词地按顺序处理，就像人阅读一样从左到右。

*核心问题*

* 容易健忘
* 没法并行

## 核心思想

不按顺序读，同时读所有词，然后计算每个词和其他词之间的关系

## 自注意力机制

*在众多信息中，有选择地关注最重要的部分*

### 自注意力计算过程

从技术角度，自注意力的计算涉及三个关键概念：Query（查询）、Key（键）、Value（值）。

可以用一个图书馆查书的比喻来理解：

Query（查询）：你想找什么？——你的搜索关键词
Key（键）：每本书的标签——用来匹配搜索
Value（值）：每本书的内容——真正有用的信息
在自注意力中：

每个词都会生成自己的 Q、K、V 三个向量
某个词的 Q 会和所有词的 K 做匹配，计算相关性分数
根据分数对所有词的 V 做加权求和，得到最终的输出

* 能处理长距离依赖：任意两个词都有直接的连接
* 可以并行计算：充分利用 GPU 的并行能力
* 关系建模灵活：每个词都和所有词建立联系

## 多头注意力机制

在实际的 Transformer 中，不是只做一次自注意力，而是同时做多次，这叫多头注意力（Multi-Head Attention）。

假设我们用 8 个头（这是常见的设置）：

把原始的 Q、K、V 向量分成 8 份
每份独立做一次自注意力计算
把 8 个头的输出拼接起来
通过一个线性变换，得到最终输出
如果画成流程图，多头注意力的处理过程大致如下：

![多头注意力的处理过程](https://javaup.chat/generated/plantuml/8027a48bb33b00f24a3a.svg)

## 位置编码

Transformer 的解决方案是位置编码（Positional Encoding）：给每个位置生成一个独特的向量，然后加到词的向量上。模型通过位置编码来理解词序（旋转位置编码）

# 整体架构

![[{AC5FB3D1-8075-4194-95F7-DCD9F24738EF}.png]]


编码器的任务是理解输入。它把输入文本转换成一系列向量，这些向量编码了输入的语义信息。

解码器的任务是生成输出。它一个词一个词地生成，每生成一个词都会参考编码器的输出（通过编码器-解码器注意力）。

在大语言模型（如 GPT）中，通常只使用解码器，因为它的任务就是根据上文生成下文，不需要单独的编码器。
